let patientId = 50;
let isEnrolled = true;
const patientInfo = {
  firstName: 'Ana',
  lastName: 'Santos',
};
const patientEmail = 'ana@email.com';

console.log(typeof patientId, typeof patientInfo, typeof isEnrolled, typeof patientEmail)

// -------------------

const base = 5;
let height = 8;

let area = base * height;
let perimeter = (base * 2) + (height * 2)
console.log(area, perimeter)